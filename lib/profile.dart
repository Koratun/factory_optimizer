import 'package:flutter/material.dart';

class GameProfile extends StatelessWidget {
  final String game;

  const GameProfile(this.game, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    ListView(children: [ListTile()],)
  }
}
